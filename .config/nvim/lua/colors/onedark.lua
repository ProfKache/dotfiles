require("onedark").setup({
	style = "deep", -- deep, dark, darker, cool, warm, warmer
	transparent = false,
	code_style = {
		comments = "italic",
		keywords = "italic",
		functions = "none",
		variables = "italic",
		string = "none",
	},
})
require("onedark").load()
